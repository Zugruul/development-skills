---
tags: [fixtures, provenance, testing]
paths: ["**"]
strength: 1
source: "#91 retro (echo-vs-printf changed the assertion bytes)"
graduated: false
created: 2026-07-08
---

Fixture bytes claiming "live captured" or "byte-exact" must be GENERATED by running the real command into a file — never hand-authored from memory (a one-character trigger difference, echo vs printf, changed the exact error text). A brief's quoted evidence is a LEAD to verify (grep git log -p and feedback archives for the verbatim string), not a source to copy — if you can't find it, label the fixture RECONSTRUCTED honestly and cite where YOU found what you did. Design the header/payload format and extraction convention BEFORE writing corpus content, not after.

Related: [[deterministic-repro-fast]]
